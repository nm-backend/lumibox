# Аудит LumiBox

Статус проверок. Цель — каждый пункт либо закрыт кодом и доказан прогоном,
либо честно помечен как ожидающий внешних credentials (настройки вне репозитория).

Легенда: ✅ закрыто и проверено · 🟡 код готов, ждёт credentials на проде ·
⚪️ вне объёма, решение осознанное.

## Сводка

| # | Направление | Статус | Доказательство |
|---|-------------|--------|----------------|
| 1 | Тесты и покрытие | ✅ | 764 теста (3 skip), OK; coverage 95%; порог CI 90% |
| 2 | Статический анализ (ruff) | ✅ | `ruff check apps config scripts` — 0 ошибок |
| 3 | Статическая типизация (mypy) | ✅ | `mypy apps` — 0 ошибок, 145 файлов |
| 4 | Миграции | ✅ | `makemigrations --check --dry-run` — no changes |
| 5 | Боевые проверки Django | ✅ | `check --deploy --fail-level WARNING` — 0 issues |
| 6 | CI/CD | ✅ | Все локальные эквиваленты 13 шагов проходят; workflow использует PostgreSQL 18 + Redis 8. Статус удалённого запуска подтверждается после push |
| 7 | Деплой-конфиги | ✅ | Dockerfile, render.yaml, render.paid.yaml, fly.toml, railway.json, compose |
| 8 | Живые страницы | ✅ | `scripts/smoke_pages.py` — 25 URL, все 200 |
| 9 | Каталог и разделы | ✅ | Витрины `/new/ /popular/ /top/ /premieres/ /year/<год>/`, франшизы |
| 10 | Фильтры | ✅ | Тип, жанр, страна, год, диапазон лет, рейтинг, качество, возраст, озвучка |
| 11 | Поиск | ✅ | Один сервис на сайт и API; название, оригинал, описания, жанр, страна, студия, люди |
| 12 | Плеер и озвучки | ✅ | PlaybackSource: файл или внешний плеер, выбор озвучки, серии по сезонам |
| 13 | Прогресс просмотра | ✅ | Позиция в секундах, продолжение с места, троттлинг раз в 15 с |
| 14 | Обсуждения | ✅ | Комментарии с ответами (один уровень), модерация, API |
| 15 | Отзывы и рейтинг | ✅ | Оценка 1–10, денормализованный рейтинг, три пути пересчёта |
| 16 | Личная библиотека | ✅ | Избранное, «смотреть позже», история; гонко-безопасные переключатели |
| 17 | SEO | ✅ | sitemap (включая разделы, франшизы, годы), JSON-LD с крю и счётчиками, OG, canonical |
| 18 | Доступность | ✅ | Вкладки с ролями и клавиатурой, фокус-ловушка в модалке, skip-link, aria-live; axe 0 нарушений: 24 публичные + 4 авторизованные страницы × светлая/тёмная × 1920/390 px |
| 19 | Безопасность заголовков | ✅ | CSP, HSTS, nosniff, X-Frame-Options (`check --deploy` 0) |
| 20 | Внешние плееры | ✅ | Адрес обязан пройти белый список хостов (`PlaybackSource.clean`) |
| 21 | Brute-force и рейт-лимиты | ✅ | django-axes; 60/мин гость, 300/мин авторизованный |
| 22 | Медиа и файлы | ✅ | WebP, лимиты загрузки, удаление файлов при удалении записи, Range 206/416 |
| 23 | Кэширование | ✅ | Инвалидация «поколениями», прогрев главной, ленивый сайдбар |
| 24 | Производительность БД | ✅ | select_related/prefetch, индексы, тесты против N+1 |
| 25 | Темы и мобильная версия | ✅ | Светлая тема работает на всех ширин, тема применяется до отрисовки; стиль Apple HIG поверх прежней структуры: единый Inter (700–800 у заголовков), стеклянная шапка blur(20px), карточки radius 20px, кнопки-пилюли, нейтральный скроллбар; сетка, отступы и навигация — как в исходной вёрстке (активный пункт меню — нейтральная черта под текстом) |
| 26 | API и документация | ✅ | DRF + drf-spectacular, `/api/docs/`, тест «каждый маршрут описан в схеме» |
| 27 | Аналитика и consent | ✅ | GA4 только после явного согласия; без GA_ID — 0 байт трекинга |
| 28 | Email и сброс пароля | 🟡 | SMTP-бэкенд включается по `EMAIL_HOST`, на проде ждёт credentials |
| — | R2-хранилище (прод) | 🟡 | Код готов, ждёт `AWS_*` / `CLOUDFLARE_R2_PUBLIC_URL` |
| — | Sentry (прод) | 🟡 | Интеграция готова, ждёт `SENTRY_DSN` |
| — | Каталоги переводов | ✅ | `scripts/generate_translations.py` собирает сам, без gettext: 471 запись × ru/en, 0 непереведённых |
| — | Подписка на сериал, запрос фильма, PWA | ⚪️ | Вне объёма осознанно |

## Доказательства (последний прогон)

```
python manage.py test apps                → Ran 764, OK (skipped=3)
coverage report                            → TOTAL 95%
ruff check apps config scripts             → All checks passed!
mypy apps                                  → Success: no issues found in 145 files
makemigrations --check --dry-run           → No changes detected
check --deploy --fail-level WARNING        → System check identified no issues
scripts/check_branding.py                  → чисто
scripts/smoke_pages.py                     → все 25 URL отдали 200
python scripts/generate_translations.py    → ru/en: 471 запись, 0 непереведённых
axe через CDP (4 конфигурации)             → 0 violations: light/dark 1920, light 390, авторизованные
```

## Что требует внешнего окружения

1. **Каталоги переводов собираются скриптом, а не `makemessages`.** В окружении
   нет GNU gettext (и Docker не запущен), поэтому генерацию каталогов делает
   `python scripts/generate_translations.py`: он читает единственный список
   строк в `STRINGS`, подставляет переводы из словарей `EN`/`KY` и пишет
   `django.po` + `django.mo` для обоих языков (сейчас 471 запись,
   непереведённых нет). Обновление после правок шаблонов:
   ```bash
   python scripts/generate_translations.py
   ```
   Строки, добавленные в шаблоны через `{% trans %}`, сперва нужно дописать
   в `STRINGS` (и, если нужно, в словари переводов).

2. **Поиск на SQLite регистрозависим для кириллицы.** `icontains` уходит
   в `LIKE`, а он в SQLite регистронезависим только для латиницы: «начало»
   не найдёт «Начало». Бой и `docker-compose` работают на PostgreSQL (`ILIKE`),
   где всё верно. Касается только запуска через `run_local_windows.bat`.

3. **SMTP, Sentry, R2 и GA** включаются появлением своих переменных
   на хостинге, без правки кода. Точный список — в `.env.example`.

## Production: что проверено отдельно

| Что | Результат |
|---|---|
| Миграции с нуля | Пустая база проходит все 58 миграций без ошибок |
| `collectstatic` боевыми настройками | 195 файлов, 470 post-processed через `CompressedManifestStaticFilesStorage` — сборка образа не сломается |
| `Dockerfile` | Обе инструкции `CMD` разбираются как корректный JSON-массив |
| Gunicorn | `gthread`, 3 воркера × 4 потока, таймаут 300 с — одинаково во всех пяти точках запуска |
| ALLOWED_HOSTS / CSRF | При пустом `DJANGO_ALLOWED_HOSTS` домен платформы подхватывается сам; `CSRF_TRUSTED_ORIGINS` собирается с `https://` и без localhost |
| Healthcheck | `/healthz/` исключён из SSL-редиректа, статус определяет только база |
| Redis / Celery | Без `REDIS_URL` кэш в памяти процесса, задачи eager — сайт поднимается |
| R2 | Адреса стабильные и неподписанные (проверено на подставных credentials) |
| Сквозные сценарии | 18 проверок одним прогоном: гость → каталог → поиск → фильтр → раздел → страница записи → регистрация → избранное → оценка → прогресс (продолжение с 12:34) → комментарий → ответ → выход → черновик отдаёт 404 |
| Мобильная вёрстка | 320 и 375 px: горизонтального скролла нет ни на главной, ни в каталоге, ни на странице записи |
| Светлая тема на мобильном | Фон переключается `#07080d` → `#f2f4f8` (до правки был прибит к `#111111`) |

## Почему видео в R2 раздаётся публично

Каталог открытый: смотреть можно без регистрации и подписки. Объект в бакете
и так `public-read`, поэтому подпись адреса ничего не охраняет — она лишь
делает ссылку временной.

При этом до правки поведение зависело от одной незаполненной переменной:
без `CLOUDFLARE_R2_PUBLIC_URL` django-storages подписывал адреса, и подпись
жила час. Зритель, вернувшийся к поставленному на паузу фильму позже,
получал отказ при перемотке — Range-запрос уходил с протухшей подписью.
Теперь `querystring_auth: False` задан явно, и поведение одинаково в обоих
случаях.

Если каталог станет платным, приватность делается не настройкой хранилища:
нужно снять `public-read`, включить `querystring_auth` и отдавать ссылку через
вьюху, проверяющую права. Заготовка уже есть — префикс `private_media/`
в `apps/core/media_serving.py` закрыт от прямой раздачи.

## Урок: локально зелёно ≠ CI зелёный

Полгода CI оставался красным, и это не замечали: локальный прогон на SQLite
проходил полностью. Ломались шесть тестов раздачи медиа, и только
на PostgreSQL.

Причина оказалась в лишней строке в самих тестах — `response.close()`.

Раздача отдаёт `FileResponse`. Тест-клиент Django оборачивает его тело
в `closing_iterator_wrapper`: дочитав итератор, обёртка закрывает ответ
сама и делает это безопасно — снимает `close_old_connections`, закрывает,
возвращает обработчик на место (`django/test/client.py:120-126`).

Явный `response.close()` поверх слал `request_finished` второй раз, когда
обработчик уже вернулся, и тот закрывал соединение с базой. `TestCase`
держит открытую транзакцию — она обрывалась. Падал при этом **не тот тест,
где стояла лишняя строка**, а следующий: на первом же обращении к базе
в `setUp`. Отсюда и сложность — симптом был в одном месте, причина в другом.

На SQLite соединение переживает закрытие, поэтому локально всё оставалось
зелёным.

Два вывода:
1. **Смотреть на CI, а не только на локальный прогон.** Разница SQLite и
   PostgreSQL — не «мелочь окружения», именно на ней и разошлись зелёный
   ноутбук с красным пайплайном.
2. **Не «дочищать» за фреймворком.** Django уже закрывает потоковый ответ
   в тестах правильно; ручное закрытие ломало то, что работало.

## Известные ограничения

- **Видео без R2 раздаёт сам Django.** Это работает (Range-запросы, 206/416),
  но упирается в число потоков gunicorn. Для боя с реальной нагрузкой R2
  обязателен — тогда видео идёт мимо приложения, прямо из бакета.
- **Дубли CSS-правил** `.site-header`, `.search`, `.logo` (наслоение «скина»
  поверх базовой раскладки) не сведены: чистый рефактор без выгоды для зрителя,
  но с реальным риском регрессии вёрстки, которую нечем проверить автоматически.
- **Прогресс хранит одно место на запись**, а не отметки по каждой серии:
  показываем мы только «продолжить», а история «серия за серией» стоила бы
  записи на каждом таймкоде.
- **Бесплатный тариф Render** засыпает после 15 минут тишины, а его Postgres
  живёт 30 дней. Для демонстрации это годится, для боя — `render.paid.yaml`.
