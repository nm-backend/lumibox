# Окружение разработчика: всё из base + инструменты разработки.
-r base.txt

# Линтер и форматтер. Ловит неиспользуемые импорты, мёртвые переменные
# и отступы — то, что глазами на ревью пропускается.
# Запуск: ruff check apps config
ruff==0.15.22

# Статическая проверка типов. Ловит несовместимые присваивания и
# несуществующие атрибуты в аннотированном коде. Конфиг — в mypy.ini.
# Запуск: mypy apps
mypy==2.3.0

# Git-хуки: единые правила для всех, кто трогает репозиторий.
# Установка: pre-commit install, запуск: pre-commit run --all-files
pre-commit==4.6.1

# Измерение покрытия тестами. Показывает, какие строки ни разу
# не выполнялись — именно так нашлась команда seed_catalog с 0% покрытия.
# Запуск: coverage run --source=apps manage.py test apps && coverage report
coverage==7.15.2

# Selenium: browser automation для healthcheck-тестов плеера.
selenium==4.46.0

# WebDriver Manager: авто-загрузка chromedriver под версию Chrome.
webdriver-manager==4.1.2
