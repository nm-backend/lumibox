# Боевое окружение: всё из base + то, что нужно только на сервере.
-r base.txt

# WSGI-сервер. Сам Django через runserver в продакшене работать не должен.
gunicorn==23.0.0

# Отдача статики самим приложением. Нужна, пока перед Django нет Nginx;
# с Nginx статику быстрее отдаёт он.
whitenoise==6.11.0

# S3-совместимое хранилище для медиа (Cloudflare R2, AWS S3, MinIO).
# Файлы не теряются при перезапуске на Render.
django-storages[boto3]==1.14.5
