# Зависимости, нужные проекту в любом окружении.
# Версии закреплены жёстко: одинаковое окружение локально и на сервере.

Django==5.2.16

# Драйвер PostgreSQL. Используем psycopg 3, а не устаревший psycopg2:
# у psycopg2 нет готовых сборок под Python 3.14.
psycopg[binary]==3.3.4

# Чтение настроек и паролей из .env вместо хранения их в коде.
django-environ==0.14.0

# Обработка изображений. Нужна Django для ImageField — постеров фильмов.
Pillow==12.3.0

# REST API.
djangorestframework==3.17.1

# Документация API: OpenAPI-схема, Swagger UI и ReDoc.
# Схема собирается из самих сериализаторов и вьюсетов, поэтому не расходится
# с кодом — в отличие от документации, написанной руками.
drf-spectacular==0.30.0

# Фильтры для API. На сайте фильтрует своя форма — здесь
# django-filter избавляет от её повторения в каждом ViewSet.
django-filter==26.1

# Фоновые задачи: пересчёт рейтингов вне цикла запроса.
celery==5.6.3

# Argon2 — самый устойчивый к GPU-атакам алгоритм хеширования паролей.
# Требуется для PASSWORD_HASHERS = [Argon2PasswordHasher, ...]
argon2-cffi==25.1.0

# HTTP-клиент серверной интеграции Vibix. Явная production-зависимость:
# раньше requests приезжал только транзитивно вместе с Selenium в dev,
# поэтому Celery и команды синхронизации не импортировались в боевом образе.
requests==2.34.2

# Клиент Redis. Redis служит и кэшем, и брокером для Celery.
# Без него проект работает: кэш падает на память процесса,
# а задачи выполняются на месте.
redis==8.0.1

# Защита от brute-force на форме входа. Блокирует аккаунт после N неудачных
# попыток, отслеживает IP, умеет разблокировать из админки.
# Без неё злоумышленник перебирает пароли без ограничений.
django-axes==8.3.1


